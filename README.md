# Patching Report Dashboard

This Streamlit app reads a patching report CSV and allows users to view, filter, search, and visualize installed software packages by hostname and package.

## Run locally

```bash
pip install -r requirements.txt
streamlit run app.py
